
<nav class="navbar navbar-default  sidebar" role="navigation">
    <div class="container-fluid">
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">


                @if (User.IsInRole("UserAdmin") || User.IsInRole("MainAdmin"))
                {
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            <i class="fa fa-user"></i>
                            <span style="padding-right: 10px"> مدیریت کاربری</span>
                        </a>
                        <ul class="dropdown-menu forAnimate" role="menu">
                            <li>
                                @Html.ActionLink("لیست کاربران", "index", "UsersAdmin", new { @area = "Authentication" },new { @class = "BNazanin" } )
                            </li>
                            <li>
                                @Html.ActionLink("کاربر جدید", "Create", "UsersAdmin", new { @area = "Authentication" }, new { @class = "BNazanin" })
                            </li>
                            <li>
                                @Html.ActionLink("لیست نقش ها", "index", "RolesAdmin", new { @area = "Authentication" }, new { @class = "BNazanin" })
                            </li>
                            <li>
                                @Html.ActionLink("نقش جدید", "Create", "RolesAdmin", new { @area = "Authentication" }, new { @class = "BNazanin" })
                            </li>

                        </ul>
                    </li>
                }

                @if (User.IsInRole("ChartAdmin") || User.IsInRole("MainAdmin"))
                {
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            <i class="fa fa-chain"></i>
                            <span style="padding-right: 10px"> چارت سازمانی </span>
                        </a>
                        <ul class="dropdown-menu forAnimate" role="menu">
                            <li>
                                @Html.ActionLink("نمایش چارت", "index", "Chart", new { @area = "Admin" }, new { @class = "BNazanin" })
                            </li>
                        </ul>
                        
                        
                        

                    </li>
                }

                @if (User.IsInRole("WorkFlowAdmin") || User.IsInRole("MainAdmin"))
                {

                    <li>
                        <a href="#">
                            <i class="fa fa-sticky-note-o"></i>
                            <span style="padding-right: 10px"> گردش اطلاعات</span>

                        </a>
                    </li>
                }

                @if (User.IsInRole("NewsAdmin") || User.IsInRole("MainAdmin"))
                {
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            <i class="fa fa-newspaper-o"></i>
                            <span style="padding-right: 10px">خبرگزاری</span>
                        </a>

                        <ul class="dropdown-menu forAnimate" role="menu">
                            <li>
                                @Html.ActionLink("گروه خبری", "index", "NewsGroup", new { @area = "Admin" }, new { @class = "BNazanin" })
                            </li>
                            <li>
                                @Html.ActionLink("اخبار ", "index", "news", new { @area = "Admin" }, new { @class = "BNazanin" })
                            </li>

                        </ul>
                    </li>
                }


                @if (User.IsInRole("ShopAdmin") || User.IsInRole("MainAdmin"))
                {

                    <li>
                        <a href="#">
                            <i class="fa fa-dollar"></i>
                            <span style="padding-right: 10px"> فروش</span>

                        </a>
                    </li>
                }


            </ul>
        </div>
    </div>
</nav>